version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.development
    entrypoint: sh -c "composer install && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=80"
    ports:
      - "8001:80"
    volumes:
      - ./application:/app